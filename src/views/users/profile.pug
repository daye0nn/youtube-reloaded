extends ../base.pug
include ../mixins/video

block content 
  div.profile_cover
    img(src="/" + user.avatarUrl width="100" height="100").profile_img
    h3=user.name
  div.video_grid
    each video in user.videos 
      +video(video)
    else 
      span.empty_message Sorry Nothing Found.